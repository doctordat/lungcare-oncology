export const BIOMARKER_EVIDENCE={label:'CAP / IASLC / AMP molecular testing guideline',note:'Older molecular-testing standard used only for test-structure concepts; not presented as a 2026 guideline.'};
export function nsclcPathology(pathologyType=''){return pathologyType==='Adenocarcinoma'||pathologyType==='Squamous cell carcinoma'||pathologyType.startsWith('NSCLC')}
export function biomarkerConsistency(state){const d=state.doctorReview||{},c=d.treatmentContext||{},stage=d.stagingInput?.calculatedStage||'',issues=[];if(d.diagnosisConfirmed&&d.pathologyReviewed&&!nsclcPathology(d.pathologyType||''))issues.push('Treatment routing hiện chỉ hỗ trợ NSCLC; pathology hiện tại nằm ngoài scope.');if(/^IV/.test(stage)&&d.diseaseExtent!=='advanced')issues.push('TNM stage IV nhưng phạm vi bệnh chưa được ghi là tiến xa / di căn.');if(/^(I|II|III)/.test(stage)&&d.diseaseExtent==='advanced')issues.push('TNM stage I–III nhưng phạm vi bệnh đang được ghi là tiến xa / di căn; cần review consistency.');const checklist=d.biomarkers||{},names=Object.keys(checklist);if(d.biomarkerStatus==='complete'){const missing=names.filter(name=>!checklist[name]);if(missing.length)issues.push(`Biomarker status = complete nhưng checklist còn thiếu: ${missing.join(', ')}.`)}if(c.driverStatus==='present'&&!String(c.driverName||'').trim())issues.push('Driver status = present nhưng chưa ghi alteration đã xác nhận.');if(c.driverStatus==='none'&&d.biomarkerStatus!=='complete')issues.push('Không thể khóa driver status = none khi biomarker workup chưa complete.');return issues}
export function legacyBiomarkerSnapshot(state){const d=state.doctorReview||{};return {status:d.biomarkerStatus||'pending',tests:Object.fromEntries(Object.entries(d.biomarkers||{}).map(([name,done])=>[name,{status:done?'resulted_unknown':'pending',result:null}])),pdl1:{status:d.biomarkers?.['PD-L1']?'resulted_unknown':'pending',tps:null},migrationNote:'Legacy boolean true means checklist/test previously marked complete; it is never interpreted as positive or negative.'}}

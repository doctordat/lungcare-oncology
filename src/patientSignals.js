const KEY='lungcare.phase1.state';const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}};const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function active(s){return s?.cases?.find(x=>x.id===s.activeCaseId)}
function severity(t={}){return ['fatigue','nausea','diarrhea','skin','neuropathy'].some(k=>t[k]==='severe')||t.adherence==='stopped'}
function panel(c){const t=c.patientTracking||{},p=t.previsit||{},tox=t.toxicity||{},last=[...(t.checkins||[])].reverse()[0],urgent=last?.report&&(last.report.hemoptysis||last.report.confusion||last.report.chestPain||last.report.dyspnea==='Tăng nhiều'),priority=urgent||severity(tox);const media=last?.attachments||[];return `<section class="panel patient-signals ${priority?'priority':''}" data-patient-signals><div class="panel-head"><div><div class="eyebrow">Patient-reported signals</div><h2>Thông tin bệnh nhân tự khai</h2></div><span class="${priority?'required':'state-chip'}">${priority?'Cần ưu tiên review':'Theo dõi'}</span></div><div class="signal-grid"><div><span>Trước khám</span><strong>${esc(p.concern||'Chưa khai')}</strong><small>${p.updatedAt?new Date(p.updatedAt).toLocaleString('vi-VN'):'Chưa cập nhật'}</small></div><div><span>Thuốc</span><strong>${esc(tox.adherence||'Chưa cập nhật')}</strong><small>${severity(tox)?'Có tín hiệu độc tính/adherence cần xem':'Chưa có tín hiệu nặng được ghi nhận'}</small></div><div><span>Triệu chứng gần nhất</span><strong>${esc(last?.summary||'Chưa có symptom check-in')}</strong><small>${last?.at?new Date(last.at).toLocaleString('vi-VN'):'Chưa cập nhật'}</small></div></div>${p.change?`<div class="signal-note"><b>Thay đổi từ lần trước</b><span>${esc(p.change)}</span></div>`:''}${tox.note?`<div class="signal-note"><b>Ghi chú độc tính</b><span>${esc(tox.note)}</span></div>`:''}${media.length?`<div class="signal-media"><b>Media bệnh nhân gửi</b>${media.map(m=>m.kind==='photo'?`<a href="${m.dataUrl}" target="_blank" rel="noreferrer"><img src="${m.dataUrl}" alt="Ảnh bệnh nhân tự gửi"></a>`:`<audio controls src="${m.dataUrl}"></audio>`).join('')}</div>`:''}<p class="helper-text">Patient-reported data dùng để hỗ trợ triage và chuẩn bị quyết định; cần clinician xác minh trước khi dùng cho xử trí.</p></section>`}
function inject(){const s=read(),c=active(s),root=document.querySelector('.workspace');if(!root||!c||!['doctor','nurse'].includes(s.activeRole)||document.querySelector('[data-patient-signals]'))return;const marker=root.querySelector('.patient-strip');if(marker)marker.insertAdjacentHTML('afterend',panel(c))}
new MutationObserver(inject).observe(document.documentElement,{childList:true,subtree:true});queueMicrotask(inject);

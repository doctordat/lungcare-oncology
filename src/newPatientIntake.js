const KEY='lungcare.phase1.state';
const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}};
const write=s=>localStorage.setItem(KEY,JSON.stringify(s));
const esc=(v='')=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const active=s=>s?.cases?.find(x=>x.id===s.activeCaseId)||null;
const intake=c=>c?.newPatientIntake||{status:'draft',profile:{},history:{},medications:'',allergySelfReport:'unknown',chiefConcern:'',voiceTranscript:'',attachments:[],submittedAt:null,nurseReview:null};
function fileInput(id,label,accept='image/*,.pdf'){return `<label class="intake-upload"><span>＋</span><strong>${label}</strong><small>Chụp ảnh hoặc chọn tệp</small><input id="${id}" type="file" accept="${accept}" capture="environment"></label>`}
function attachmentList(x){return (x.attachments||[]).length?`<div class="intake-attachment-list">${x.attachments.map(a=>`<div><span>${esc(a.kind)}</span><strong>${esc(a.name)}</strong><small>${Math.round((a.size||0)/1024)} KB</small></div>`).join('')}</div>`:''}
function form(c){const x=intake(c),p=x.profile||{},h=x.history||{};return `<section class="new-patient-intake" data-new-patient-intake><div class="intake-hero"><div><div class="eyebrow">Bệnh nhân mới · Bước đầu tiên</div><h1>Khai hồ sơ trước khi đến khám</h1><p>Bạn có thể gõ, nói hoặc chụp tài liệu. Điều dưỡng sẽ kiểm tra lại trước khi chuyển bác sĩ.</p></div><div class="intake-progress"><b>1</b><span>Khai hồ sơ</span><i></i><b>2</b><span>Nurse xác minh</span><i></i><b>3</b><span>Bác sĩ khám</span></div></div><div class="intake-grid"><section class="intake-card"><div class="intake-card-head"><span>01</span><div><h2>Thông tin cá nhân</h2><p>Thông tin cơ bản để định danh khi tiếp nhận.</p></div></div><div class="form-grid"><label>Họ và tên<input id="npiName" value="${esc(p.name||c.patient?.name||'')}"></label><label>Ngày sinh<input id="npiDob" type="date" value="${esc(p.dob||'')}"></label><label>Số điện thoại<input id="npiPhone" inputmode="tel" value="${esc(p.phone||'')}"></label><label>Giới tính<select id="npiSex"><option value="">Chọn</option><option value="male" ${p.sex==='male'?'selected':''}>Nam</option><option value="female" ${p.sex==='female'?'selected':''}>Nữ</option><option value="other" ${p.sex==='other'?'selected':''}>Khác</option></select></label><label class="full">Địa chỉ<input id="npiAddress" value="${esc(p.address||'')}"></label></div>${fileInput('npiIdCard','Chụp CCCD / giấy tờ định danh')}</section><section class="intake-card voice-feature"><div class="intake-card-head"><span>02</span><div><h2>Lý do đi khám</h2><p>Nói tự nhiên như đang kể với bác sĩ, hoặc nhập bằng bàn phím.</p></div></div><div class="voice-box"><button class="voice-start" data-action="npi-voice"><span>●</span>Bắt đầu nói</button><small>Nếu trình duyệt hỗ trợ, lời nói sẽ được chuyển thành chữ ngay trong ô bên dưới.</small></div><textarea id="npiConcern" placeholder="Ví dụ: Tôi ho khoảng 2 tháng, gần đây khó thở hơn...">${esc(x.chiefConcern||x.voiceTranscript||'')}</textarea><label class="audio-upload">Hoặc gửi ghi âm<input id="npiVoiceFile" type="file" accept="audio/*" capture></label></section><section class="intake-card"><div class="intake-card-head"><span>03</span><div><h2>Tiền căn & dị ứng</h2><p>Không chắc thì chọn “Không rõ”; điều dưỡng sẽ hỏi lại.</p></div></div><div class="form-grid"><label>Bệnh nền / tiền căn<textarea id="npiHistory" placeholder="Tăng huyết áp, đái tháo đường, lao, phẫu thuật...">${esc(h.medical||'')}</textarea></label><label>Tiền căn gia đình<textarea id="npiFamily" placeholder="Ung thư, bệnh di truyền...">${esc(h.family||'')}</textarea></label><label>Hút thuốc<textarea id="npiSmoking" placeholder="Ví dụ: 20 gói-năm, đã ngưng 2 năm">${esc(h.smoking||'')}</textarea></label><label>Dị ứng thuốc<select id="npiAllergy"><option value="unknown" ${x.allergySelfReport==='unknown'?'selected':''}>Không rõ</option><option value="no" ${x.allergySelfReport==='no'?'selected':''}>Chưa từng biết dị ứng</option><option value="yes" ${x.allergySelfReport==='yes'?'selected':''}>Có dị ứng</option></select></label><label class="full">Nếu có, mô tả dị ứng / phản ứng<textarea id="npiAllergyNote">${esc(x.allergyNote||'')}</textarea></label></div></section><section class="intake-card"><div class="intake-card-head"><span>04</span><div><h2>Thuốc & hồ sơ đang có</h2><p>Chụp vỏ thuốc, toa cũ, CT/X-quang hoặc xét nghiệm đã có.</p></div></div><label>Thuốc đang dùng<textarea id="npiMeds" placeholder="Tên thuốc nếu nhớ; nếu không nhớ có thể chụp ảnh bên dưới">${esc(x.medications||'')}</textarea></label><div class="intake-upload-grid">${fileInput('npiMedPhoto','Thuốc / toa đang dùng')}${fileInput('npiImaging','CT / X-quang / PET')}${fileInput('npiLabs','Xét nghiệm / giải phẫu bệnh')}${fileInput('npiOther','Hồ sơ khác','image/*,.pdf')}</div>${attachmentList(x)}</section></div><div class="intake-submit-bar"><div><strong>Kiểm tra trước khi gửi</strong><span>Điều dưỡng sẽ xác minh lại danh tính, dị ứng, thuốc và tài liệu. Không tự suy diễn thông tin còn thiếu.</span></div><button class="primary-btn large" data-action="npi-submit">Gửi hồ sơ cho điều dưỡng</button></div></section>`}
function waiting(c){const x=intake(c),r=x.nurseReview||{},reviewing=x.status==='reviewing',verified=x.status==='verified';return `<section class="new-patient-intake status" data-new-patient-intake><div class="intake-hero"><div><div class="eyebrow">Hồ sơ bệnh nhân mới</div><h1>${verified?'Điều dưỡng đã xác minh hồ sơ':reviewing?'Điều dưỡng đang kiểm tra hồ sơ':'Đã gửi hồ sơ'}</h1><p>${verified?'Hồ sơ cơ bản đã được xác minh và sẵn sàng chuyển sang bước bác sĩ khám.':'Bạn chưa cần khai lại. Nếu điều dưỡng cần làm rõ, họ sẽ liên hệ theo quy trình của cơ sở.'}</p></div><div class="status-orb ${verified?'ok':'waiting'}">${verified?'✓':'…'}</div></div><div class="intake-status-steps"><div class="done"><b>✓</b><span>Bệnh nhân đã khai</span><small>${x.submittedAt?new Date(x.submittedAt).toLocaleString('vi-VN'):''}</small></div><div class="${reviewing||verified?'done':'active'}"><b>${reviewing||verified?'✓':'2'}</b><span>Nurse xác minh</span><small>${reviewing?'Đang xử lý':verified?'Hoàn tất':'Đang chờ'}</small></div><div class="${verified?'active':''}"><b>3</b><span>Bác sĩ khám</span><small>${verified?'Sẵn sàng':'Chưa mở'}</small></div></div>${verified?`<div class="success-callout"><strong>Đã xác minh</strong> · Dị ứng: ${esc(r.allergyStatus||'chưa ghi')} · Thuốc: ${r.medicationsReviewed?'đã đối chiếu':'chưa đối chiếu'}</div>`:''}</section>`}
async function toAttachment(file,kind){if(!file)return null;if(file.size>900000){alert('Demo hiện giới hạn mỗi tệp khoảng 900 KB.');return null}return await new Promise(resolve=>{const fr=new FileReader();fr.onload=()=>resolve({id:crypto.randomUUID(),kind,name:file.name,size:file.size,type:file.type,dataUrl:fr.result,at:new Date().toISOString()});fr.readAsDataURL(file)})}
function inject(){const s=read(),c=active(s);if(!s||!c||s.activeRole!=='patient'||document.querySelector('[data-new-patient-intake]'))return;const root=document.querySelector('.patient-home');if(!root)return;const x=intake(c),locked=x.status!=='verified';root.classList.toggle('new-intake-required',locked);root.insertAdjacentHTML('afterbegin',x.status==='draft'?form(c):waiting(c))}
function save(s,c){const i=s.cases.findIndex(x=>x.id===c.id);if(i>=0){s.cases[i]=c;write(s)}}
document.addEventListener('click',async e=>{const b=e.target.closest('[data-action]');if(!b)return;const a=b.dataset.action,s=read(),c=active(s);if(!s||!c||s.activeRole!=='patient')return;if(a==='npi-voice'){e.preventDefault();const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return alert('Trình duyệt này chưa hỗ trợ chuyển giọng nói thành chữ. Bạn vẫn có thể gửi file ghi âm.');const rec=new SR();rec.lang='vi-VN';rec.interimResults=false;rec.onresult=ev=>{const text=[...ev.results].map(r=>r[0].transcript).join(' '),ta=document.getElementById('npiConcern');if(ta)ta.value=(ta.value+' '+text).trim()};rec.start()}if(a==='npi-submit'){e.preventDefault();const x=intake(c),files=[['npiIdCard','CCCD'],['npiMedPhoto','Thuốc/toa'],['npiImaging','Hình ảnh'],['npiLabs','Xét nghiệm'],['npiOther','Khác'],['npiVoiceFile','Ghi âm']],added=[];for(const [id,kind] of files){const f=document.getElementById(id)?.files?.[0],att=await toAttachment(f,kind);if(att)added.push(att)}const name=document.getElementById('npiName')?.value.trim()||'';if(!name)return alert('Vui lòng nhập họ tên.');const now=new Date().toISOString();c.newPatientIntake={...x,status:'submitted',profile:{name,dob:document.getElementById('npiDob')?.value||'',phone:document.getElementById('npiPhone')?.value.trim()||'',sex:document.getElementById('npiSex')?.value||'',address:document.getElementById('npiAddress')?.value.trim()||''},history:{medical:document.getElementById('npiHistory')?.value.trim()||'',family:document.getElementById('npiFamily')?.value.trim()||'',smoking:document.getElementById('npiSmoking')?.value.trim()||''},chiefConcern:document.getElementById('npiConcern')?.value.trim()||'',medications:document.getElementById('npiMeds')?.value.trim()||'',allergySelfReport:document.getElementById('npiAllergy')?.value||'unknown',allergyNote:document.getElementById('npiAllergyNote')?.value.trim()||'',attachments:[...(x.attachments||[]),...added],submittedAt:now};c.patient={...c.patient,name};c.unreadAlerts=(c.unreadAlerts||0)+1;c.events=[...(c.events||[]),{id:crypto.randomUUID(),type:'NEW_PATIENT_INTAKE_SUBMITTED',role:'patient',text:'Bệnh nhân mới đã gửi hồ sơ khai trước khám.',at:now}];save(s,c);location.reload()}},true);
new MutationObserver(inject).observe(document.documentElement,{childList:true,subtree:true});queueMicrotask(inject);
const STORAGE_KEY='lungcare.phase1.state';
function read(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'null')}catch{return null}}
function write(s){localStorage.setItem(STORAGE_KEY,JSON.stringify(s))}
document.addEventListener('click',e=>{const b=e.target.closest('[data-action="save-followup"]');if(!b)return;const state=read();if(!state||!Array.isArray(state.cases))return alert('Không đọc được dữ liệu demo.');const idx=state.cases.findIndex(x=>x.id===state.activeCaseId);if(idx<0)return alert('Không tìm thấy ca hiện tại.');const c=state.cases[idx],r=c.patientEducation?.symptomReport||{},rel=[...(c.planReleases||[])].reverse().find(x=>x.status==='released')||null,status=document.getElementById('followupStatus')?.value||'routine',note=document.getElementById('followupNote')?.value.trim()||'';const snapshot={id:crypto.randomUUID(),at:new Date().toISOString(),status,note,planVersion:rel?.version||null,planReleaseId:rel?.id||null,symptoms:{dyspnea:r.dyspnea||'',pain:r.pain??'',fever:Boolean(r.fever),hemoptysis:Boolean(r.hemoptysis),confusion:Boolean(r.confusion),chestPain:Boolean(r.chestPain)}};const event={id:crypto.randomUUID(),type:'FOLLOWUP_SNAPSHOT',role:'doctor',text:`Đã lưu mốc follow-up: ${status}${rel?` · patient plan v${rel.version}`:''}.`,at:snapshot.at};state.cases[idx]={...c,followups:[...(c.followups||[]),snapshot],events:[...(c.events||[]),event]};write(state);location.reload()});

const KEY='lungcare.phase1.state';
const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}};
const write=s=>localStorage.setItem(KEY,JSON.stringify(s));
const esc=(v='')=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const active=s=>s?.cases?.find(x=>x.id===s.activeCaseId)||null;
const severeToxicity=t=>['fatigue','nausea','diarrhea','skin','neuropathy'].some(k=>t?.[k]==='severe')||t?.adherence==='stopped';
const urgentReport=r=>Boolean(r?.submittedAt)&&(r.hemoptysis||r.confusion||r.chestPain||r.dyspnea==='Tăng nhiều');
function molecular(c){const b=c.doctorReview?.biomarkerResults||{},positive=Object.entries(b.tests||{}).filter(([,x])=>x?.status==='positive').map(([g,x])=>`${g}${x.alteration?`: ${x.alteration}`:''}`);const tps=b.pdl1?.status==='resulted'&&b.pdl1?.tps!==''?`${b.pdl1.tps}%`:'Chưa có';return{positive,tps}}
function summary(c){const t=c.patientTracking||{},tox=t.toxicity||{},r=c.patientEducation?.symptomReport||{},m=c.clinicalMonitoring||{},img=[...(m.imaging||[])].reverse()[0],mol=molecular(c),alerts=[];if(urgentReport(r))alerts.push('Triệu chứng cảnh báo từ bệnh nhân');if(severeToxicity(tox))alerts.push('Độc tính/adherence cần ưu tiên review');if((c.unreadAlerts||0)>0)alerts.push(`${c.unreadAlerts} tín hiệu chưa đọc`);return{alerts,tox,r,m,img,mol}}
function panel(c){const s=summary(c),d=c.doctorReview||{},stage=d.stagingInput?.calculatedStage||'',tx=s.m.currentTreatment||{},img=s.img;const response=img?.response||'Chưa có đánh giá';return `<section class="doctor-cockpit" data-doctor-cockpit><div class="cockpit-head"><div><div class="eyebrow">Doctor clinical cockpit</div><h2>Nắm ca trong 60 giây</h2><p>Dữ liệu theo dõi bệnh nhân + điều trị + hình ảnh + genomic để chuẩn bị quyết định. Không phải bệnh án điện tử.</p></div><span class="cockpit-status ${s.alerts.length?'danger':'ok'}">${s.alerts.length?`${s.alerts.length} ưu tiên`:'Không có cảnh báo mới'}</span></div>${s.alerts.length?`<div class="cockpit-alerts">${s.alerts.map(x=>`<div><b>!</b><span>${esc(x)}</span></div>`).join('')}</div>`:''}<div class="cockpit-grid"><article class="cockpit-card"><span>Điều trị hiện tại</span><strong>${esc(tx.name||'Chưa ghi')}</strong><small>${esc([tx.cycle,tx.status].filter(Boolean).join(' · ')||'Chưa cập nhật trạng thái')}</small><button data-action="cockpit-edit-treatment">Cập nhật điều trị</button></article><article class="cockpit-card"><span>CT / đáp ứng</span><strong>${esc(response)}</strong><small>${img?`${esc(img.modality)} · ${esc(img.date||'chưa ngày')}`:'Chưa có lần đánh giá hình ảnh'}</small><button data-action="cockpit-add-imaging">Nhập CT & đáp ứng</button></article><article class="cockpit-card"><span>Genomic / biomarker</span><strong>${s.mol.positive.length?esc(s.mol.positive.join('; ')):'Chưa có alteration positive cấu trúc'}</strong><small>PD-L1 TPS: ${esc(s.mol.tps)}</small></article><article class="cockpit-card"><span>Stage & decision</span><strong>${stage?`Stage ${esc(stage)}`:'Chưa có TNM 9 hoàn chỉnh'}</strong><small>${esc(d.treatmentContext?.decision?.optionTitle||'Chưa lưu clinician treatment decision')}</small></article></div><div class="cockpit-two"><article class="cockpit-card wide"><div class="cockpit-card-head"><div><span>Thuốc & độc tính</span><strong>${esc(s.tox.adherence||'Chưa có patient adherence check-in')}</strong></div><em class="${severeToxicity(s.tox)?'danger':'neutral'}">${severeToxicity(s.tox)?'Ưu tiên':'Theo dõi'}</em></div><div class="tox-mini"><span>Mệt: ${esc(s.tox.fatigue||'—')}</span><span>Buồn nôn/nôn: ${esc(s.tox.nausea||'—')}</span><span>Tiêu chảy: ${esc(s.tox.diarrhea||'—')}</span><span>Da: ${esc(s.tox.skin||'—')}</span><span>Tê bì: ${esc(s.tox.neuropathy||'—')}</span></div><p>${esc(s.tox.note||c.intake?.medicationSafety?.currentMedications||'Chưa có ghi chú thuốc/độc tính.')}</p></article><article class="cockpit-card wide"><span>Clinical synthesis · demo</span><strong>${s.alerts.length?'Có tín hiệu cần xử lý trước workflow thường quy.':'Chưa ghi nhận tín hiệu cấp bách từ dữ liệu hiện có.'}</strong><p>${img?`Hình ảnh gần nhất: ${esc(img.response)}${img.note?` — ${esc(img.note)}`:''}. `:''}${s.mol.positive.length?`Molecular positive: ${esc(s.mol.positive.join('; '))}. `:''}${tx.name?`Điều trị đang ghi nhận: ${esc(tx.name)}.`:'Chưa ghi điều trị hiện tại.'}</p><small>Đây là tổng hợp quy tắc từ dữ liệu demo, không phải chẩn đoán hay y lệnh AI.</small></article></div><div class="cockpit-history"><div><span>Imaging timeline</span><strong>${(s.m.imaging||[]).length} mốc</strong></div>${(s.m.imaging||[]).length?[...(s.m.imaging||[])].reverse().slice(0,4).map(x=>`<div class="imaging-row"><span>${esc(x.date||'Chưa ngày')}</span><b>${esc(x.modality||'CT')}</b><strong>${esc(x.response||'Chưa đánh giá')}</strong><small>${esc(x.note||'')}</small></div>`).join(''):'<div class="empty-state">Chưa có CT/response timeline.</div>'}</div></section>`}
function modal(kind,c){const m=c.clinicalMonitoring||{},tx=m.currentTreatment||{};if(kind==='treatment')return `<div class="cockpit-modal" data-cockpit-modal><div class="cockpit-dialog"><div class="panel-head"><div><div class="eyebrow">Current treatment</div><h2>Cập nhật điều trị đang theo dõi</h2></div><button data-action="cockpit-close">Đóng</button></div><label>Tên phác đồ/thuốc đang ghi nhận<input id="cockpitTxName" value="${esc(tx.name||'')}" placeholder="Clinician-entered"></label><div class="form-grid"><label>Chu kỳ / ngày<input id="cockpitTxCycle" value="${esc(tx.cycle||'')}" placeholder="VD: Cycle 3 · Day 1"></label><label>Trạng thái<select id="cockpitTxStatus"><option value="active" ${tx.status==='active'?'selected':''}>Đang điều trị</option><option value="held" ${tx.status==='held'?'selected':''}>Tạm hoãn</option><option value="completed" ${tx.status==='completed'?'selected':''}>Đã hoàn tất</option><option value="unknown" ${!tx.status||tx.status==='unknown'?'selected':''}>Chưa xác định</option></select></label></div><label>Ghi chú theo dõi<textarea id="cockpitTxNote">${esc(tx.note||'')}</textarea></label><button class="primary-btn" data-action="cockpit-save-treatment">Lưu theo dõi điều trị</button></div></div>`;return `<div class="cockpit-modal" data-cockpit-modal><div class="cockpit-dialog"><div class="panel-head"><div><div class="eyebrow">Imaging response</div><h2>Nhập CT & đánh giá đáp ứng</h2></div><button data-action="cockpit-close">Đóng</button></div><div class="clinical-note">Bác sĩ nhập kết luận/đánh giá. App không tự đo tổn thương hoặc tự tính RECIST.</div><div class="form-grid"><label>Ngày chụp<input id="cockpitImgDate" type="date"></label><label>Loại hình ảnh<select id="cockpitImgModality"><option>CT ngực</option><option>CT ngực-bụng</option><option>PET-CT</option><option>Khác</option></select></label><label>Đánh giá đáp ứng<select id="cockpitImgResponse"><option>Chưa xác định</option><option>Đáp ứng hoàn toàn</option><option>Đáp ứng một phần</option><option>Bệnh ổn định</option><option>Tiến triển</option><option>Đáp ứng hỗn hợp</option></select></label></div><label>Kết luận / ghi chú<textarea id="cockpitImgNote" placeholder="Tổn thương đích, tổn thương mới, nhận xét radiology/clinician..."></textarea></label><button class="primary-btn" data-action="cockpit-save-imaging">Lưu mốc CT</button></div></div>`}
function inject(){const st=read(),c=active(st),root=document.querySelector('.workspace');if(!root||!c||st.activeRole!=='doctor'||document.querySelector('[data-doctor-cockpit]'))return;const signals=root.querySelector('[data-patient-signals]'),marker=signals||root.querySelector('.patient-strip');if(marker)marker.insertAdjacentHTML('afterend',panel(c))}
new MutationObserver(inject).observe(document.documentElement,{childList:true,subtree:true});queueMicrotask(inject);
document.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b)return;const a=b.dataset.action;if(!a?.startsWith('cockpit-'))return;const st=read(),idx=st?.cases?.findIndex(x=>x.id===st.activeCaseId);if(idx==null||idx<0)return;const c=st.cases[idx];if(a==='cockpit-close'){document.querySelector('[data-cockpit-modal]')?.remove();return}if(a==='cockpit-edit-treatment'||a==='cockpit-add-imaging'){document.body.insertAdjacentHTML('beforeend',modal(a==='cockpit-edit-treatment'?'treatment':'imaging',c));return}if(a==='cockpit-save-treatment'){const now=new Date().toISOString(),currentTreatment={name:document.getElementById('cockpitTxName')?.value.trim()||'',cycle:document.getElementById('cockpitTxCycle')?.value.trim()||'',status:document.getElementById('cockpitTxStatus')?.value||'unknown',note:document.getElementById('cockpitTxNote')?.value.trim()||'',updatedAt:now};st.cases[idx]={...c,clinicalMonitoring:{...(c.clinicalMonitoring||{}),currentTreatment},events:[...(c.events||[]),{id:crypto.randomUUID(),type:'CURRENT_TREATMENT_UPDATED',role:'doctor',text:'Bác sĩ cập nhật điều trị đang theo dõi trong Clinical Cockpit.',at:now}]};write(st);location.reload();return}if(a==='cockpit-save-imaging'){const date=document.getElementById('cockpitImgDate')?.value||'',response=document.getElementById('cockpitImgResponse')?.value||'Chưa xác định';if(!date)return alert('Cần nhập ngày chụp CT/hình ảnh.');const now=new Date().toISOString(),item={id:crypto.randomUUID(),date,modality:document.getElementById('cockpitImgModality')?.value||'CT ngực',response,note:document.getElementById('cockpitImgNote')?.value.trim()||'',enteredAt:now};st.cases[idx]={...c,clinicalMonitoring:{...(c.clinicalMonitoring||{}),imaging:[...((c.clinicalMonitoring||{}).imaging||[]),item]},events:[...(c.events||[]),{id:crypto.randomUUID(),type:'IMAGING_RESPONSE_RECORDED',role:'doctor',text:`Bác sĩ ghi mốc ${item.modality}: ${response}.`,at:now}]};write(st);location.reload()}}
,true);
